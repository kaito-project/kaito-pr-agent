name: PR-Agent E2E tests

on:
  workflow_dispatch:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  # schedule:
  #   - cron: '0 2 * * *' # Run nightly at 2 AM UTC

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  pr_agent_job:
    runs-on: ubuntu-latest
    name: PR-Agent E2E GitHub App Test
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Docker Buildx
        uses: docker/setup-buildx-action@v3

      - id: build
        name: Build dev docker
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./docker/Dockerfile
          push: false
          load: true
          tags: kaito-project/kaito-pr-agent:test
          cache-from: type=gha,scope=dev
          cache-to: type=gha,mode=max,scope=dev
          target: test

      - name: Generate GitHub App Token
        id: generate_token
        uses: actions/create-github-app-token@v1
        with:
          app-id: ${{ secrets.APP_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}

      - id: test1
        name: E2E test github app
        run: |
          docker run -e GITHUB.USER_TOKEN=${{ steps.generate_token.outputs.token }} --rm kaito-project/kaito-pr-agent:test pytest -v tests/e2e_tests/test_github_app.py

      # - id: test2
      #   name: E2E gitlab webhook
      #   run: |
      #     docker run -e gitlab.PERSONAL_ACCESS_TOKEN=${{ secrets.TOKEN_GITLAB }} --rm kaito-project/kaito-pr-agent:test pytest -v tests/e2e_tests/test_gitlab_webhook.py

      # - id: test3
      #   name: E2E bitbucket app
      #   run: |
      #     docker run -e BITBUCKET.USERNAME=${{ secrets.BITBUCKET_USERNAME }}  -e BITBUCKET.PASSWORD=${{ secrets.BITBUCKET_PASSWORD }} --rm kaito-project/kaito-pr-agent:test pytest -v tests/e2e_tests/test_bitbucket_app.py
